This task continues from a previous coding-agent session.

First, do not implement.

1. Confirm pwd, Git root, branch, short HEAD, `git status --short`, and `git diff --stat`.
2. Read applicable instruction files and `/workspace/acme-dashboard/HANDOFF.md`.
3. Read only the detail artifacts required for the smallest next step, if HANDOFF.md lists any.
4. Inspect the files listed under "Files to inspect first", "Required Reading", and "Change Manifest".
5. Compare HANDOFF.md with the actual working tree.
6. If they conflict, trust disk and report the mismatch.
7. If SAFE_FOR_NEW_SESSION is not yes, stop after the report unless I explicitly instruct you how to proceed.

Report:
1. Loaded instructions:
2. Repo state:
3. Handoff consistency:
4. Detail artifacts read:
5. Missing or conflicting paths:
6. First implementation step:

Proceed only if I explicitly asked for implementation and the manifest/disk verification is safe.
